<h1>ホーム画面になります</h1>

<% @items.each do |item| %>
  <% if item.image.attached? %>
    <%= link_to item_path(item) do %>
      <%= image_tag(item.image.variant(resize_to_limit: [200, 200])) %>
    <% end %>
  <% else %>
    <p>No image</p>
  <% end %>
<% end %>
